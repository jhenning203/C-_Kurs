#include <iostream>

/*  1.) Variablenname kann nicht mit einer Zahl starten
    2.) Ohne Leerzeichen
    3.) Einzigartigkeit
*/


int main() //Einstiegs und Endpunkt des Computerprogramms
{
    // Der Variablenname steht immer rechts(Deklarerung), der Wert steht immer links(Initialisierung)

    int my_int_value = 2; // int für Ganze Zahlen
    int my_int_value_2;   // Hier nur deklariert aber nicht initialisiert

    float my_float_value = 3.5; // float für Kommazahelen
    return 0;
}
